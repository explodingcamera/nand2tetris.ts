{
  "name": "nand2tetris2020",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "assembler": "yarn run --silent ts-node assembler",
    "translator": "yarn run --silent ts-node translator",
    "compiler": "yarn run --silent ts-node compiler"
  },
  "workspaces": {
    "packages": [
      "assembler",
      "translator",
      "compiler"
    ]
  },
  "dependencies": {
    "@tsconfig/node12": "^1.0.7",
    "@types/node": "^14.14.5",
    "typescript": "4.0.5",
    "ts-node": "9.0.0"
  }
}
