{
  "name": "nand2tetris2020",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "assembler": "yarn run --silent ts-node packages/assembler",
    "translator": "yarn run --silent ts-node packages/translator",
    "compiler": "yarn run --silent ts-node packages/compiler"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "@tsconfig/node12": "^1.0.7",
    "@types/node": "^14.14.6",
    "ts-node": "9.0.0",
    "typescript": "4.0.5"
  }
}
